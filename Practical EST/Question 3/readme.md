Promise   → An object that represents a task that will finish in the future (success or failure).
.then()   → Runs when the promise   succeeds   (resolved) and gives you the result.
.catch()   → Runs when the promise   fails   (rejected) and gives you the error.

